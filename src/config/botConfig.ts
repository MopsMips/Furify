// Konfiguration für deinen Musikbot

// Zeit in Millisekunden, nach der der Bot den Voice-Channel verlässt, wenn nichts mehr läuft
export const AUTO_LEAVE_TIMEOUT = 30_000; // 30 Sekunden

// (Optional) Maximale Anzahl Songs in der Queue
export const MAX_QUEUE_LENGTH = 50;

// (Optional) Debug-Modus aktivieren
export const DEBUG_MODE = true;
